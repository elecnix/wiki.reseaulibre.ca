No node has been clearly established yet. 
[[!if test="(enabled(template) or enabled(edittemplate))
and enabled(inline)" then="""
Here is the list of documented nodes:

[[!inline pages="nodes/* and !*/*/* and !*/discussion" feeds=no archive=yes sort=title rootpage=nodes postform="yes" postformtext="Add a new node named:"]]
"""]]

The links between nodes is documented in [[links]].

## Meta

I have contacted the
[techno-viking](http://techno-viking.com/posts/ikiwiki-maps/) to see
if he could share his IkiWiki/OpenLayers integration to document the
nodes locations and so on... Awaiting his feedback. -- [[TheAnarcat]]
2011-10-10
